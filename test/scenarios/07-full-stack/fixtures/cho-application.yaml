apiVersion: chorister.dev/v1alpha1
kind: ChoApplication
metadata:
  name: scen07-myapp
spec:
  owners:
    - platform-admin@example.com
  policy:
    compliance: essential
    promotion:
      requiredApprovals: 0
  domains:
    - name: payments
      sensitivity: internal
