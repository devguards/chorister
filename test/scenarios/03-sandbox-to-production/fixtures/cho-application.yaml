apiVersion: chorister.dev/v1alpha1
kind: ChoApplication
metadata:
  name: scen03-myapp
spec:
  owners:
    - platform-admin@example.com
  policy:
    compliance: essential
    promotion:
      requiredApprovals: 1
      allowedRoles:
        - org-admin
  domains:
    - name: payments
      sensitivity: internal
