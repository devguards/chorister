apiVersion: chorister.dev/v1alpha1
kind: ChoApplication
metadata:
  name: scen08-myapp
  namespace: cho-system
spec:
  domains:
    - name: payments
      sensitivity: internal
      compliance: standard
      runtimeDetection: full
