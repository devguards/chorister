# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/chorister.dev_choapplications.yaml
- bases/chorister.dev_chocomputes.yaml
- bases/chorister.dev_chodatabases.yaml
- bases/chorister.dev_choqueues.yaml
- bases/chorister.dev_chocaches.yaml
- bases/chorister.dev_chostorages.yaml
- bases/chorister.dev_chonetworks.yaml
- bases/chorister.dev_chodomainmemberships.yaml
- bases/chorister.dev_chopromotionrequests.yaml
- bases/chorister.dev_choclusters.yaml
- bases/chorister.dev_chosandboxes.yaml
- bases/chorister.dev_chovulnerabilityreports.yaml
- bases/chorister.chorister.dev_choapplications.yaml
- bases/chorister.chorister.dev_chonetworks.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.
#configurations:
#- kustomizeconfig.yaml
