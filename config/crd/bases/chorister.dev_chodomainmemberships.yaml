---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: chodomainmemberships.chorister.dev
spec:
  group: chorister.dev
  names:
    kind: ChoDomainMembership
    listKind: ChoDomainMembershipList
    plural: chodomainmemberships
    singular: chodomainmembership
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.application
      name: Application
      type: string
    - jsonPath: .spec.domain
      name: Domain
      type: string
    - jsonPath: .spec.identity
      name: Identity
      type: string
    - jsonPath: .spec.role
      name: Role
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ChoDomainMembership is the Schema for the chodomainmemberships
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ChoDomainMembershipSpec defines the desired state of ChoDomainMembership.
            properties:
              application:
                description: application is the parent application name.
                type: string
              domain:
                description: domain is the domain to grant access to.
                type: string
              expiresAt:
                description: |-
                  expiresAt is when this membership expires. Required for restricted domains
                  and regulated applications.
                format: date-time
                type: string
              identity:
                description: identity is the user identity (e.g. email address).
                type: string
              oidcGroup:
                description: oidcGroup is the OIDC group name (when source=oidc-group).
                type: string
              role:
                description: role is the access level.
                enum:
                - org-admin
                - domain-admin
                - developer
                - viewer
                type: string
              source:
                default: manual
                description: source indicates how the membership was created.
                enum:
                - manual
                - oidc-group
                type: string
            required:
            - application
            - domain
            - identity
            - role
            type: object
          status:
            description: ChoDomainMembershipStatus defines the observed state of ChoDomainMembership.
            properties:
              conditions:
                description: conditions represent the current state of the membership.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: phase is the membership lifecycle state.
                enum:
                - Active
                - Expired
                type: string
              roleBindingRef:
                description: roleBindingRef is the name of the created RoleBinding.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
